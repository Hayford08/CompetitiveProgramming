#ifdef __cplusplus
#define _Alignof(type) __alignof__(type)
#endif
#include <bits/stdc++.h>
using namespace std;

bool multi = true;
inline void solve(){
  int n;  
  cin >> n;
  return;
}

int main(){
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  cout.tie(nullptr);

  int t = 1;
  if (multi) {
    cin >> t;
  }
  
  while (t--) {
    solve();
  }
  return 0;
}
